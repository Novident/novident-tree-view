name: novident_tree_view 
description: "A flexible Novident Flutter library for visualizing hierarchical structures with full control over nodes. It allows you to create customizable trees without rigid architectures."
version: 1.0.0
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.16.0
  collection: ^1.19.1
  provider: ^6.1.4
  novident_nodes: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
